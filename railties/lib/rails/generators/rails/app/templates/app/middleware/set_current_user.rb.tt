# Development/internal only.
# Replace with JWT/session validation for production.
class SetCurrentUser
  def initialize(app)
    @app = app
  end

  def call(env)
    Current.user_role = env["HTTP_X_USER_ROLE"]
    Current.user_id = env["HTTP_X_USER_ID"]&.to_i
    @app.call(env)
  end
end
